# FastAPI & Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
pgvector>=0.2.0
greenlet>=3.0.0  # required by SQLAlchemy's asyncio extension (AsyncSession/AsyncEngine)

# Pydantic
pydantic>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.0.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Face Recognition
face-recognition>=1.3.0
numpy>=1.26.0
pillow>=10.0.0

# Notifications
resend>=2.0.0
pywebpush>=2.0.0

# Utils
python-dotenv>=1.0.0
tzdata>=2024.1  # zoneinfo needs this on minimal images (python:3.11-slim lacks OS tzdata)
setuptools>=70.0.0,<81.0.0  # Required for face_recognition_models pkg_resources in Python 3.12+

# Testing
pytest>=9.1.1
pytest-asyncio>=0.23.0
httpx>=0.26.0
aiosqlite>=0.20.0  # in-memory async SQLite engine for real-DB constraint tests
