# Tasks: Fix RBAC Users Access Model

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 1,600-2,300 (Phase 3: ~950-1,300; Phase 4: ~650-1,000) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR1 (merged) -> PR2 `#32` (open) -> PR3 migration+scope+backend enforcement -> PR4 permission-request workflow -> PR5 Angular |
| Delivery strategy | auto-chain |
| Chain strategy | pending (maintainer decision; feature-branch-chain assumed) |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|-----------------------|------------------|--------------------|
| 1 | Canonical roles, migration, bootstrap recovery, helpers | PR1 (merged) | `cd backend && pytest tests/test_rbac_access_model.py` | N/A — merged, no re-run needed | n/a, already shipped |
| 2 | Users/Roles/Permissions enforcement | PR2 `#32` (open) | `cd backend && pytest tests/test_rbac_access_model.py` | N/A — under review | n/a, already open |
| 3 | Role split, `user_scope_assignments`, `positions.canonical_role`, scope admin surface, coarse-gate replacement (tasks 3.1-3.12, 3.15) | PR3 | `cd backend && pytest tests/test_rbac_access_model.py tests/test_user_scope_assignment.py` | `alembic upgrade head && alembic downgrade -1` against a seeded staging DB | revert PR3 branch; migration downgrade restores `ADMINISTRATIVO`-only state |
| 4 | Two-stage permission-request state machine (tasks 3.13-3.14) | PR4 | `cd backend && pytest tests/test_permission_requests.py` | Direct `httpx` calls exercising stage-1/stage-2 transitions against seeded scope rows | revert PR4 branch; PR3 endpoints/model unaffected |
| 5 | Angular role/permission model, guards, scope admin UI (Phase 4) | PR5 | `cd frontend && npm test -- --watch=false` | `ng serve` manual walkthrough of coordinador/secretaría/director views | revert PR5 branch; backend enforcement stays authoritative |

## Phase 1: Foundation / Migration

- [x] 1.1 RED: add canonical role, unknown-role denial, and bootstrap detection tests in `backend/tests/test_rbac_access_model.py`.
- [x] 1.2 GREEN: update `backend/app/core/config.py`, `backend/app/models/user.py`, and `backend/alembic/versions/*_canonical_rbac_roles.py`.
- [x] 1.3 RED/GREEN: test/update `backend/create_admin_user.py` and `backend/create_admin.sql` for env bootstrap repair.
- [x] 1.4 RED/GREEN: test/add permission and object-access helpers in `backend/app/api/deps.py`.

## PR1 Foundation Blocker Fixes

- [x] B1 RED/GREEN: enforce `protect_bootstrap_admin_mutation()` in users endpoints for update, deactivate via `is_active`, delete, and password change.
- [x] B2 RED/GREEN: enforce `ensure_can_assign_role()` in users and roles endpoints so only bootstrap `ADMIN` can create, assign, or replace `DEV`.
- [x] B3 RED/GREEN: preserve deploy-safe access for migrated business admins while avoiding total `has_permission()` access for non-bootstrap `ADMIN`.
- [x] B4 RED/GREEN: validate `LEGACY_ADMIN_FALLBACK_ROLE` against a non-system business-role allowlist.
- [x] B5 RED/GREEN: make Alembic downgrade map canonical user roles back to legacy values without deleting role assignments or canonical role rows.
- [x] B6 RED/GREEN: make `ADMIN` system-only by blocking `ADMIN` assignment through users and RBAC role assignment backoffice paths, including bootstrap actors.
- [x] B7 RED/GREEN: reserve the configured bootstrap admin email from users API reassignment paths case-insensitively and keep bootstrap admin mutation server-side only.
- [x] B8 RED/GREEN: enforce `ensure_can_assign_role()` and reserved bootstrap email protection in `/auth/register` so business admins cannot create `ADMIN` or `DEV` users.

## Phase 2: Users / RBAC APIs

- [x] 2.1 RED: add users API tests for bootstrap `ADMIN` list/update/deactivate/reset/delete denial.
- [x] 2.2 GREEN: update `backend/app/api/v1/endpoints/users.py` to hide/protect bootstrap admin and reject permission-matrix writes.
- [x] 2.3 RED/GREEN: test/enforce DEV assignment; only bootstrap `ADMIN` may create or assign `DEV`.
- [x] 2.4 RED/GREEN: test/update `backend/app/api/v1/endpoints/roles.py` and `permissions.py` for DEV-only RBAC internals.

## Phase 3: Role Split, Scope Model, Backend Enforcement

- [x] 3.1 RED: update `backend/tests/test_rbac_access_model.py` legacy-mapping parametrize (lines ~124-126: `coordinador`→`COORDINADOR`, `secretaria`→`SECRETARIA`, `supervisor`→`COORDINADOR`) and `CANONICAL_ROLES`. Spec: rbac-access-model "Known legacy roles map to split canonical roles".
- [x] 3.2 GREEN: `backend/app/models/user.py` — add `COORDINADOR`/`SECRETARIA` enum values + legacy aliases, deprecate `ADMINISTRATIVO` as non-assignable, retarget `LEGACY_ROLE_MAPPING["coordinador"/"secretaria"/"supervisor"]` (D1/D2). Spec: "Canonical Roles", "Deprecated ADMINISTRATIVO is not assignable through any API".
- [x] 3.2b RED/GREEN: enforce `ASSIGNABLE_ROLE_VALUES` in `ensure_can_assign_role()` (`backend/app/api/deps.py`) so `ADMINISTRATIVO` cannot be assigned via any users/auth endpoint; add direct-API denial tests for `PATCH /users/{id}` (`users_endpoint.update_user`) and `POST /auth/register` (`auth_endpoint.register`) in `backend/tests/test_rbac_access_model.py`. Spec: "Canonical Roles" — "Deprecated ADMINISTRATIVO is not assignable through any API". Standalone follow-up closing the CRITICAL gap flagged in `verify-pr3-slice1-report.md`.
- [x] 3.3 RED: create `backend/tests/test_user_scope_assignment.py` — CHECK constraint (exactly one of department/location/director target), unique index on the triple, and a reclassification-ladder unit test (surviving `roles.name` → `positions.canonical_role` → fallback `COORDINADOR` + audit row, no scope). Spec: "Organizational Scope Assignment", "Ambiguous migrated ADMINISTRATIVO user lands on unscoped COORDINADOR and is audited".
- [x] 3.4 GREEN: create `backend/app/models/user_scope_assignment.py` (`UserScopeAssignment`, D4) and add `canonical_role` column to `backend/app/models/position.py` (D6).
- [x] 3.5 GREEN: create `backend/alembic/versions/<rev>_split_administrativo_and_scopes.py` stacking on `202606101200` — enum values, D3 reclassification ladder + audit table row, `user_scope_assignments`, `positions.canonical_role`, new permission rows (`user_scopes.manage`, `employees.manage.catedratico`, `attendance.mark.self`, stage permissions), reversible downgrade merging `COORDINADOR`/`SECRETARIA` back to `ADMINISTRATIVO`. Spec: "Legacy Role Migration Compatibility".
- [x] 3.4b RED/GREEN: replace the broken `uq_user_scope_assignments_triple` plain UNIQUE constraint (proven non-functional in 3.3-3.5 — NULL-padded nullable columns never collide under ANSI SQL) with three partial unique indexes (`uq_user_scope_assignments_department`, `_location`, `_director`), each `UNIQUE (user_id, <target>) WHERE <target> IS NOT NULL`, in `backend/app/models/user_scope_assignment.py` and `backend/alembic/versions/202606151200_split_administrativo_and_scopes.py` (`upgrade()` + `downgrade()`). Spec: "Organizational Scope Assignment" (uniqueness of scope assignment). Design: D4.
- [x] 3.6 RED/GREEN: narrow `LEGACY_ADMIN_FALLBACK_ALLOWED_ROLES` to `{DECANO, DUEÑO}` in `backend/app/core/config.py` (D9). **Behavior change to shipped Phase-1 B3/B4 fallback** — add a regression test proving `DIRECTOR`/`ADMINISTRATIVO` are rejected as fallback targets.
- [x] 3.7 RED/GREEN: add `resolve_user_scopes`, `assert_request_scope`, `require_teacher_position` to `backend/app/api/deps.py`; mark `get_current_secretaria_or_above`/`get_current_coordinador_or_above`/`get_current_active_admin` deprecated (D10). Spec: "Missing scope assignment fails closed".
- [x] 3.8 RED/GREEN: create `backend/app/api/v1/endpoints/user_scopes.py` — `user_scopes.manage`-gated CRUD for coordinador/director facultad-sede and secretaría-director assignments; register router. Spec: "Coordinador/Secretaría scope assignment is persisted", "Only authorized actor manages scope assignments".
- [x] 3.9 RED/GREEN: `backend/app/api/v1/endpoints/employees.py` — replace `get_current_secretaria_or_above` with `require_permission("employees.manage.catedratico")` + `require_teacher_position`. Spec: "SECRETARIA creates/edits catedrático employees only", "SECRETARIA cannot manage non-teaching employees".
- [x] 3.9b RED/GREEN: `backend/app/api/v1/endpoints/employees.py` — close the gap flagged (not fixed) by task 3.11: `delete_employee` was the only remaining action in this file still using `get_current_active_admin`, letting `DECANO`/`DUEÑO` delete employees (spec: "Business Top Role Boundaries — DECANO and DUEÑO cannot perform operational write actions"). Replaced with `require_permission("employees.delete")`, reusing the pre-existing `employees.delete` code seeded by `9f8e7d6c5b4a` and already granted to `ADMIN`/`DEV` via `202606101200`'s unfiltered cross-join (no new migration needed). Audited the rest of the file: `list_employees`/`get_employee` never used `get_current_active_admin` (auth-only reads, same convention as `schedules.py`); `create_employee`/`update_employee`'s `require_permission("employees.manage.catedratico")` gate from 3.9 is unchanged (regression-locked by a dedicated test). Tests: `backend/tests/test_employees_delete_gate.py` (new, 8 tests). Full regression: 224 passed (was 216), zero failures.
- [x] 3.10 RED/GREEN: `backend/app/api/v1/endpoints/schedules.py`, `departments.py`, `positions.py`, `locations.py` — replace `get_current_coordinador_or_above`/`get_current_secretaria_or_above` with `require_permission` + `assert_request_scope`; `COORDINADOR`/`DIRECTOR` read-only. Spec: "Module Access Boundaries" (DIRECTOR/COORDINADOR scenarios). `schedules.py` portion done in a prior call (all 4 read endpoints gained `get_current_user` auth, 2 had none; all 9 write endpoints swapped to `require_permission("schedules.create"/"update"/"delete")`). **This call**: `departments.py`/`positions.py`/`locations.py` — all 6 read endpoints (`list_departments`, `get_department`, `list_positions`, `get_position`, `list_locations`, `get_location`) gained `get_current_user` auth (had none — same pre-existing gap as `schedules.py`); all 9 write endpoints (create/update/delete ×3 modules) swapped `get_current_secretaria_or_above`/`get_current_active_admin` for `require_permission("<module>.create"/"update"/"delete")`, reusing the pre-existing `departments.*`/`positions.*` permission codes seeded by `9f8e7d6c5b4a`. Discovered and fixed a real gap: no `locations.*` permission code existed anywhere in the codebase (unlike `departments`/`positions`/`schedules`), which would have made `require_permission("locations.*")` unsatisfiable for every actor except the hidden bootstrap `ADMIN` — added `backend/alembic/versions/202606161200_add_locations_permissions.py` seeding the 4 `locations.*` codes and granting them to `ADMIN`/`DEV` (mirroring `202606101200`'s cross-join, which predated these rows). No `assert_request_scope` call added to any of the four files — design.md's Corrected Role Matrix has no Departments/Positions/Locations/Schedules column, so read-scoping was left unimplemented rather than guessed at (same open ambiguity as the `schedules.py` slice).
- [x] 3.11 RED/GREEN: `backend/app/api/v1/endpoints/settings.py` and `faces.py` — replace `get_current_active_admin` with `require_permission`; deny `DECANO`/`DUEÑO` writes. Spec: "Business Top Role Boundaries — DECANO and DUEÑO cannot perform operational write actions".
- [x] 3.12 RED/GREEN: `backend/app/api/v1/endpoints/attendance.py` — `DECANO`/`DUEÑO`/`DIRECTOR`/`COORDINADOR` read-only reports (deny export/edit); `CATEDRATICO` self check-in via `attendance.mark.self` restricted to own `employee_id`; `SECRETARIA` catedrático-scoped employee writes. Spec: attendance-access-control "Teacher Attendance Scope", "Administrative Attendance Access". **Read-only reports** (prior call): `list_attendance`/`list_today_attendance` swapped `get_current_user` for `require_permission("attendance.view")`; migration `202606181200` grants `attendance.view` to `DECANO`/`DUEÑO`/`DIRECTOR`/`COORDINADOR`. No export/edit/delete route exists in this file, so "deny export/edit" is structural (regression-locked by a test). **CATEDRATICO self check-in** (this call): implemented via optional-auth, not a hard `require_permission` gate — `check_in`/`check_out` stay reachable without authentication (the shared face-recognition kiosk flow, unaffected for every anonymous caller), but a new `get_optional_current_user` dependency (`backend/app/api/deps.py`) resolves a best-effort actor from an Authorization header when one happens to be present (e.g. the frontend's auth interceptor attaches one automatically on an active session, even on the guardless `/kiosk`/`/attendance` routes); when that actor holds CATEDRATICO, `_enforce_self_scope_if_catedratico` calls `assert_object_access(actor, employee_id=matched_employee.id)`, denying (403) when the face-matched employee isn't the actor's own. Other authenticated roles (e.g. an admin supervising the kiosk) are unaffected. Migration `202606191200_grant_attendance_mark_self_to_catedratico.py` grants the pre-seeded `attendance.mark.self` code to `CATEDRATICO`. **Deferred, unchanged from prior call**: `SECRETARIA` catedrático-scoped attendance reads — design.md's Corrected Role Matrix lists it, but the spec's own "Administrative Attendance Access" requirement text and this task's literal scope text do not name `SECRETARIA` for report reads (only for employee writes, already covered by 3.9); left ungranted, flagged as an open design/spec inconsistency for maintainer clarification.
- [x] 3.13 RED/GREEN: `backend/app/api/v1/endpoints/permission_requests.py` two-stage transitions — stage 1 scoped `COORDINADOR` approve/reject (`pending`→`coordinator_approved`/`rejected(stage=coordinator)`); stage 2 assigned `SECRETARIA` approve/reject requiring non-empty justification (422 if missing) (`coordinator_approved`→`approved`/`rejected(stage=director)`); `DIRECTOR` denied any transition. Spec: "Stage 1 Coordinator Review", "Stage 2 Secretaría Review", "Director Notification Visibility".
- [x] 3.14 RED/GREEN: `permission_requests.py` visibility + creation — restrict list/detail to owner, scoped `COORDINADOR`, scoped `DIRECTOR` (read-only), assigned `SECRETARIA`, `DEV`/bootstrap `ADMIN`; deny `DECANO`/`DUEÑO`; `CATEDRATICO` may only create for own `employee_id`. Spec: "Permission Request Visibility", "Permission Request Creation".
- [x] 3.15 RED/GREEN: cross-scope denial tests — `COORDINADOR`/`SECRETARIA` acting on a resource outside their assigned facultad/sede/director across `employees`, `permission_requests`, `attendance`. Spec: "Cross-scope denial tests cover organizational boundaries". `permission_requests.py` already had genuine cross-scope coverage at the stage-1/stage-2 transition layer (task 3.13's `test_out_of_scope_coordinador_denied`/`test_unassigned_secretaria_denied`); this task added the missing read/visibility-layer counterpart (3 new tests: scoped `COORDINADOR`/`DIRECTOR` denied cross-facultad detail view, assigned `SECRETARIA` denied wrong-director detail view). `employees.py` and `attendance.py` have no facultad/sede/director scope dimension in their current implementation (position-only gate for `employees.py` per design D6, already tested in 3.9; permission-gate-only reads with no object-scope filter for `attendance.py`, an already-flagged open design ambiguity from 3.10/3.12) — no cross-scope test was invented for either, per this task's own explicit instruction not to fabricate a scope check that doesn't exist.

## Phase 4: Frontend Permission Visibility

- [x] 4.1 RED/GREEN: `frontend/src/app/core/models/user.model.ts` and `role.model.ts` — replace legacy lowercase `UserRole` union with canonical roles (incl. `coordinador`/`secretaria`), add `UserScopeAssignment` model. Spec: rbac-access-model "Canonical Roles".
- [x] 4.2 RED/GREEN: `frontend/src/app/core/models/permission-request.model.ts` — add stage fields (`coordinator_approved`, `rejection_stage`, `director_notes`/justification) matching the two-stage state machine. Spec: permission-request-workflow "Stage 1/2 Coordinator/Secretaría Review". **Investigation finding**: the two-stage fields this task's own text asks for (`status` union incl. `coordinator_approved`, `rejection_stage`, `coordinator_notes`/`director_notes`, `coordinator_reviewed_by/_at`, `director_reviewed_by/_at`, `rejection_reason`) were **already present** in this file (committed pre-existing at `17f5ac3`/`3209f88`, predating this SDD change — the backend's `permission_requests` table/columns already existed before Phase 3/4, per design.md's own "on top of the existing permission_requests columns" framing). Verified field-for-field against the real `PermissionRequestResponse` Pydantic schema (`backend/app/schemas/permission_request.py`, not guessed) and found the one genuine gap: `start_time`, `end_time`, `hours_affected` existed on the backend response and on the frontend's own `PermissionRequestCreate` interface, but were missing from the `PermissionRequest` (response) interface — added as `start_time: string | null`, `end_time: string | null`, `hours_affected: number | null`. `RejectionStage` (`'coordinator' | 'director'`) already matched the backend `RejectionStage(str, PyEnum)` exactly.
- [x] 4.3a RED/GREEN: backend prerequisite for 4.3 (maintainer decision, unblocks the frontend gap flagged by 4.3 below) — expose the current user's granted permission codes via `/auth/me`. Added `AuthMeResponse(UserResponse)` (`backend/app/schemas/user.py`, `/auth/me`-only, not bolted onto shared `UserResponse` since `GET /users/`'s query doesn't eager-load `user_roles -> role -> permissions` and would break/N+1) with a `permissions: list[str]` field; added `permission_codes_for_user()` (`backend/app/api/deps.py`) computed from the already-eager-loaded relationship (`get_current_user()`'s existing `selectinload`, no new query) — mirrors `has_permission()`'s exact DB-backed grant-matching loop, deliberately WITHOUT its bootstrap-ADMIN unconditional bypass, relying instead on this codebase's established migration convention of granting every new permission code to `ADMIN`/`DEV` via `role_permissions` (documented as a known, non-silent gap if that convention is ever broken by a future migration). Wired into `backend/app/api/v1/endpoints/auth.py`'s `get_current_user_info`. Tests: `backend/tests/test_auth_me_permissions.py` (new, 10 tests — role with grants, empty grants, bootstrap `ADMIN`/`DEV` full DB-backed grant set, inactive-role-assignment exclusion, endpoint-level `/auth/me` response shape).
- [x] 4.3 RED/GREEN: `auth.service.ts`, `auth.guard.ts`, `app.routes.ts` — route guards keyed on `data.permission` for `user_scopes.manage`, `permission_requests.approve.stage1/2`, `attendance.mark.self`. **This call**: `AuthService.hasPermission(code)` (new `userPermissions` signal, populated atomically from `/auth/me`'s `permissions` field alongside `currentUser`, task 4.3a) + `permissionGuard` (new exported `CanActivateFn` in `auth.guard.ts`, reads `route.data['permission']`, checks `hasPermission()`, same async-wait-for-`/auth/me` pattern as `adminGuard`, added alongside the existing role-based guards rather than replacing them). Both have full RED/GREEN regression tests (`auth.service.spec.ts` +3, `auth.guard.spec.ts` +3). `app.routes.ts` intentionally **not wired** to any of the 3 named permission codes in this call, per investigation of the actual UI/route structure (not guessed): (1) `permission_requests.approve.stage1/2` is a **button-level** check inside `AdminPermissionRequestsComponent.canApprove()`/`canReject()` on the single shared `/admin/permission-requests` route — `COORDINADOR` (stage1), `SECRETARIA` (stage2), and read-only `DIRECTOR` all need to reach the same route, so a single `data.permission` route guard would incorrectly lock out 2 of those 3 roles; the real stage-aware fix belongs to task 4.4, not a route guard. (2) `attendance.mark.self` gates the kiosk face-recognition check-in/out **endpoints**, not a route — the `/kiosk` and `/attendance` routes are intentionally anonymous-capable per task 3.12's backend decision (`get_optional_current_user`), so adding a route guard here would break the anonymous kiosk flow; enforcement is correctly backend-only. (3) `user_scopes.manage` has no route to guard yet — `user-scopes.component.ts` does not exist (task 4.6, not started); `permissionGuard` is built and tested so 4.6 can wire it directly once that route exists. No permission code was force-fit onto a route that doesn't genuinely need it.
- [x] 4.4 RED/GREEN: `frontend/src/app/features/admin/pages/permission-requests/permission-requests.component.ts` — stage-aware approve/reject actions, mandatory justification field for stage 2, read-only view for `DIRECTOR`. Fixed the confirmed bug: `canApprove()`/`canReject()` now gate on `hasPermission('permission_requests.approve.stage1'/'.stage2')` per `req.status` (`pending` → stage1, `coordinator_approved` → stage2), mirroring the real backend `has_permission()` checks in `permission_requests.py`'s `approve_permission_request`/`reject_permission_request` exactly — not a separate frontend-only role rule. `DIRECTOR` never holds either code (migration `202606201200` grants stage1 to `COORDINADOR` only, stage2 to `SECRETARIA` only), so this alone yields the read-only view for `DIRECTOR` with no extra role check. Added `isStage2()`, `canSubmitApprove()` (disables the approve submit button until `approveNotes` is non-empty when `isStage2()`), `canSubmitReject()` (disables the reject submit button until `rejectReason` is non-empty — mirrors the schema-required `rejection_reason` for both stages). Approve modal template made stage-aware (label/placeholder/hint switch between optional stage-1 "Notas" and mandatory stage-2 "Justificación *"). Verified exact endpoint URLs/payloads against `permission_requests.py` (`PATCH /permission-requests/{id}/approve` with `{notes}`, `PATCH .../reject` with `{rejection_reason}`) — `permission-request.service.ts` already called them correctly, unchanged. Confirmed via `app.routes.ts` that `CATEDRATICO` reaches its own requests through a separate `my-requests.component.ts`, not this admin component — the "requester's own view" clause in this task's own text is N/A here.
- [ ] 4.5 RED/GREEN: `frontend/src/app/features/admin/pages/attendance/attendance.component.ts` — hide export/edit actions for `COORDINADOR`, keep read-only reporting for `DECANO`/`DUEÑO`/`DIRECTOR`/`COORDINADOR`.
- [ ] 4.6 RED/GREEN: create `frontend/src/app/features/admin/pages/user-scopes/user-scopes.component.ts` — admin UI to assign `COORDINADOR`/`DIRECTOR` to facultad/sede and `SECRETARIA` to `DIRECTOR`, wired to `user_scopes.manage`.
- [ ] 4.7 RED/GREEN: `frontend/src/app/features/admin/pages/employees/employees.component.ts` and `dashboard/dashboard.component.ts` — restrict `SECRETARIA` employee create/edit UI to catedrático positions; hide non-reporting widgets for `DECANO`/`DUEÑO`. **Note (this call)**: `users.component.ts`'s 8 canonical-role compile errors (flagged by 4.1 as this task's territory) were already fixed on the `fix-rbac-users-access-model-cross-scope-tests` branch as a compile-unblocking pass, NOT as this task's own scope — role dropdown options/defaults/display maps were casing-fixed 1:1 (same role set, no addition/removal); a real "which roles can this admin surface create/manage" decision (e.g. dropping the always-403 `ADMIN` option per B6, adding `DECANO`/`DUEÑO`/`DEV`/`ESTUDIANTE`/`PADRES`) was explicitly left undecided and deferred to this task. See apply-progress.md for full detail.

## Phase 5: Verification / Cleanup

- [ ] 5.1 Run `cd backend && pytest`.
- [ ] 5.2 Run `cd frontend && npm test -- --watch=false`.
- [ ] 5.3 Review hardcoded legacy roles, `ADMIN_ROLES`, denial leaks, and PR boundaries.
